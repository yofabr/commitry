# commitry

## Nix development environment

This repository now includes a Nix flake.

```bash
nix develop
```

The development shell provides Node.js 22, which can be used to work on the Next.js app in `ui/`.
